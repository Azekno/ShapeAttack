#include "GameWorld.h"

//GameWorld::GameWorld(const char* textureName, float gameWorldWidth, float gameWorldHeight)
//{
//	SetWindowSize(gameWorldWidth, gameWorldHeight);
//}

GameWorld::GameWorld(float gameWorldWidth, float gameWorldHeight)
{
	SetWindowSize(gameWorldWidth, gameWorldHeight);
}